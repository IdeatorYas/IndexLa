import { FadeIn } from "@/components/ui/FadeIn";
import { HomeReadMore } from "@/components/home/HomeReadMore";
import {
  homeBody,
  homeEyebrow,
  homeH2,
  homeH3,
  homeLede,
  homeMeasure,
  homeMeasureTight,
  homeSection,
} from "@/components/home/homeRhythm";

const earnWays = [
  {
    title: "50% Execution Fees",
    body: "Earn 50% of applicable execution fees generated by your portfolios.",
  },
  {
    title: "Monthly Rewards",
    body: "Earn marketplace rewards based on applicable activity and performance criteria.",
  },
  {
    title: "Creator Tips",
    body: "Receive tips directly from your audience.",
  },
] as const;

export function CreatorsSection() {
  return (
    <section className={`${homeSection} bg-void`}>
      <div className="section-pad container-max">
        <FadeIn className="text-center">
          <p className={homeEyebrow}>10. CREATOR MARKETPLACE</p>
          <h2 className={`mt-3 ${homeH2}`}>Turn Your Thesis Into a Portfolio</h2>
          <p className={`mt-5 ${homeMeasure} ${homeBody}`}>
            Publish your strategy, build an audience and earn from your
            portfolio.
          </p>
          <p className={`mt-8 ${homeLede}`}>3 Ways to Earn</p>
        </FadeIn>

        <div className="mt-6 grid gap-4 md:grid-cols-3">
          {earnWays.map((item, i) => (
            <FadeIn key={item.title} delay={i * 0.04}>
              <article className="flex h-full flex-col items-center rounded-2xl border border-line bg-deep/55 px-5 py-6 text-center">
                <h3 className={`${homeH3} text-[clamp(1.15rem,2vw,1.4rem)]`}>
                  {item.title}
                </h3>
                <p className={`mt-3 ${homeMeasureTight} ${homeBody}`}>
                  {item.body}
                </p>
              </article>
            </FadeIn>
          ))}
        </div>

        <FadeIn className="mt-8 text-center">
          <p className="text-[1.15rem] font-semibold tracking-[-0.015em] text-ink">
            Create. Share. Earn.
          </p>
          <HomeReadMore
            href="/creators"
            label="Creator Guide →"
            className="mt-4"
          />
        </FadeIn>
      </div>
    </section>
  );
}
